# PROGRESS — 작업 현황 (다음 세션 인수인계 문서)

> 이 문서는 **다음 Claude 세션이 이 문서만 읽고도 바로 이어서 작업할 수 있도록** 쓴 현황 기록이다.
> "무엇을 왜 만드는가"는 `PRD.md`, "지금 뭐가 되어 있고 뭐가 안 되어 있는가"는 이 문서를 본다.

| 항목 | 내용 |
|---|---|
| 최종 수정 | 2026-08-20 |
| 저장소 | `C:\Users\223\Desktop\food_search` / GitHub `https://github.com/mypage1000/food_search.git` (branch `master`) |
| 현재 상태 | **1차 범위(지역·카테고리 필터 + 실시간 검색) 구현 완료.** 2차(지도·시티투어)는 착수 전 |
| 개발 서버 | `npm run dev` — 포트 3000이 다른 프로젝트(미세먼지 현황)와 겹쳐서 **3001**에서 뜬다 |

---

## 1. 구현 완료된 기능

### 홈 화면 (`/`, `app/page.tsx`)
- 지역: **서울특별시로 고정**, 구·군만 select로 선택(`lib/regions.ts`의 `findSido("11")`)
- 카테고리: `lib/categories.ts`의 8종(한식/중식/일식/양식/분식/카페·디저트/해산물/기타) 다중 선택 칩, 지역 기준 개수 병기, 0건은 비활성화만(숨기지 않음)
- 데이터: `app/api/region/route.ts` GET 호출 → **카카오 로컬 키워드 검색 API**(`category_group_code=FD6`, `query="서울특별시 {구·군} 맛집"`)를 서버에서 호출해 실시간으로 받아온다. 서울 25개 구 전부 실데이터가 나온다
- 히어로 영역에 `/search`(맛집 담기)로 가는 진입점 링크 있음

### 맛집 담기 (`/search`, `app/search/page.tsx`)
- 키워드 입력 + 카테고리 칩(클릭 시 검색어에 합성해 재검색)
- `app/api/search/route.ts` GET → 카카오 로컬 키워드 검색 API(`category_group_code=FD6`) 직접 호출
- 결과를 카드로 렌더링, 카드 클릭 시 카카오 플레이스 페이지(`place_url`)로 새 탭 이동

### 서버/보안
- `KAKAO_REST_API_KEY`(A등급, 서버 전용)는 `app/api/region/route.ts`·`app/api/search/route.ts` 안에서만 쓰이고, 브라우저 번들에 노출되지 않음을 `.next` 빌드 산출물 grep으로 여러 번 확인함
- `.gitignore`/`.env.local.example` 파일명 버그(점 누락) 수정 완료, `git init` 완료, `.env.local`은 정상적으로 git에서 무시됨

### 디자인
- Stitch로 초기 와이어프레임 제작 → 앱에 **토스(Toss) 스타일**로 구현 → 잠깐 **에어비앤비 스타일**로 바꿨다가 → **다시 토스 스타일로 롤백**해서 확정. 현재 `app/globals.css`의 프라이머리 컬러는 `#3182F6`(토스 블루)
- Agent Teams teammate 2명("Logic", "Design")을 파일 경계로 나눠 병렬 작업시키는 패턴을 이번 세션에서 여러 번 반복 사용함 (예: Design은 `globals.css`만, Logic은 `page.tsx`만)

### 데이터 소스 정리
- 애초 계획은 "문화공공데이터광장 맞춤형 API(`API_CNV_063`)로 맛집 데이터, 부족한 좌표는 카카오 REST로 지오코딩"이었으나, 문화광장 API가 계속 응답하지 않아(§3 참고) **맛집 데이터 소스 자체를 카카오 로컬 API로 대체**했다
- 그 결과 더 이상 안 쓰이는 `lib/sources/food/mock.ts`(모의 데이터), `lib/geocode.ts`(주소→좌표 지오코딩)는 삭제함. `data/geocode-cache.json`은 남아있지만 지금은 아무도 안 씀(2차에서 실제 지도 붙일 때 재사용하거나 지워도 됨)

### 커밋 이력 (최신순)
```
2184667 빈 landing.html 파일 추가
cef7f65 홈 화면도 모의 데이터 대신 카카오 로컬 API 실시간 검색으로 전환
d356b3e 디자인을 토스 스타일로 롤백, 홈에 맛집 담기 진입점 추가
435ec38 맛집 담기(카카오 로컬 검색) 기능 추가, 디자인을 에어비앤비 스타일로 전환
d2879ec 서버 지오코딩(카카오 REST API) 연동, 데이터 접근을 서버 라우트로 이전
4538876 디자인을 토스 스타일로 변경하고 지역 필터를 서울로 고정
6123410 1차 구현: 지역·카테고리 필터 기반 맛집 리스트 (지도/시티투어 제외)
```
전부 GitHub `master`에 push 완료 (로컬-원격 동기화 상태).

---

## 2. 주요 결정사항

1. **PRD v0.3에서 1차 범위를 지역·카테고리 필터 + 리스트로 한정**했다. 지도(F-01)·마커(F-02)·시티투어(F-05)·정류장 강조(F-06)는 2차로 명시적으로 미뤘다 (`PRD.md` §1.4 "구현 단계" 표 참고).
2. **맛집 데이터 소스를 문화공공데이터광장 → 카카오 로컬 API로 교체**했다. PRD 원안과 다른 방향인데, 문화광장 API가 활성화 지연인지 서버 문제인지 계속 타임아웃이라 실용적으로 우회한 것이다. 아직 문화광장 API 자체 문제는 미해결이고, 필요하면 나중에 재검증해서 어댑터로 되살릴 수 있게 `lib/sources/food/types.ts`(공용 `Restaurant` 타입)는 남겨뒀다.
3. **Next.js는 PRD가 명시한 16 대신 실존하는 15.5.23**을 썼다.
4. **디자인은 최종적으로 토스 스타일로 확정**했다 (에어비앤비 시도 후 롤백 — 사용자가 명시적으로 되돌려달라고 함).
5. **dev 서버(포트 3001)가 켜진 상태에서 `npm run build`를 동시에 돌리면 `.next` 캐시가 깨진다**는 걸 두 번 겪었다. 앞으로 build 확인이 필요하면 dev 서버를 잠깐 내리고 하거나, `npx tsc --noEmit`으로 대체할 것.
6. Agent Teams teammate를 쓸 때는 **파일 단위로 완전히 겹치지 않게 나누는 것**이 핵심 — 같은 파일을 두 팀원이 손대게 하면 plan mode 유무와 무관하게 충돌 위험이 있다. 색상 토큰(`globals.css`)과 마크업 로직(`page.tsx`)처럼 관심사가 다른 파일로 나누면 SendMessage 조율 없이도 완전 병렬이 가능하다는 걸 확인함.

---

## 3. 아직 해결하지 못한 문제

| 문제 | 상태 |
|---|---|
| **문화공공데이터광장 API(`API_CNV_063`)가 계속 504/타임아웃** | 원인 불명(키 활성화 지연 or 서버 문제). 지금은 카카오로 우회해서 당장 급하진 않지만, PRD 원안의 데이터 소스는 여전히 미검증 상태 |
| `DATA_GO_KR_SERVICE_KEY`(시티투어) 미발급 | `.env.local`에 비어있음. 2차(시티투어 레이어) 착수 전 발급 필요 |
| `NEXT_PUBLIC_KAKAO_MAP_JS_KEY` 미발급 | `.env.local`에 비어있음. 지도(F-01) 착수 전 발급 + 카카오 콘솔 도메인 등록(S-06, `localhost:3001`·배포 도메인) 필요 |
| `landing.html` 파일 | 저장소 루트에 **빈(0바이트) 파일**로 존재 (커밋 `2184667`). 누가/왜 만들었는지, 용도가 무엇인지 확인 안 됨. 다음 세션에서 사용자에게 물어보고 채우거나 지울 것 |
| `lib/regions.ts`의 구·군 코드 | 실제 행정표준코드가 아니라 임시 코드(`시도코드+순번`)다. 지금은 카카오 API에 지역명 **텍스트**로만 검색하므로 문제 없지만, 실제 공공데이터 API를 다시 쓰게 되면 진짜 코드로 교체해야 함 |
| 카카오 검색 결과 15건 고정 | `app/api/region/route.ts`, `app/api/search/route.ts` 둘 다 페이지네이션 없이 첫 페이지(최대 15건)만 가져온다. 맛집이 15개 넘는 지역/키워드는 더 못 봄 |
| 부분 실패·에러 상태(PRD F-09) 미흡 | 지금은 loading/빈 배열 정도만 처리. 카카오 API가 실패하면 사용자에게 원인 있는 에러 메시지 없이 그냥 빈 리스트로 보임 |
| 테스트 코드 없음 | 유닛/통합 테스트 전무 |

---

## 4. 다음에 해야 할 작업 (우선순위 순)

1. **`landing.html` 용도 확인** — 사용자에게 먼저 물어볼 것 (지우거나 채우거나)
2. **카카오맵 JS 키 발급 + 도메인 등록** → 2차 F-01(지도 베이스) 착수
3. **F-02 마커·클러스터링** — `/api/region`, `/api/search` 응답에 이미 `lat`/`lng`가 있으니 지도에 얹기만 하면 됨
4. **시티투어 데이터**: `DATA_GO_KR_SERVICE_KEY` 발급 → PRD §2.2 정규화(`TourCourse`/`TourStop`) → F-05(레이어 토글)·F-06(정류장 근접 강조)
5. **카카오 검색 페이지네이션/더보기** 붙이기 (현재 15건 제한 해소)
6. **에러 상태 UI 보강** (F-09) — 카카오 API 실패 시 사용자에게 보이는 메시지
7. **Vercel 배포** (PRD §8 마지막 단계) — 배포 후 카카오 콘솔에 배포 도메인 등록 잊지 말 것
8. 위 작업들이 어느 정도 쌓이면 `README.md`(PRD가 이미 "미작성"으로 언급해둔 문서) 작성

---

## 5. 새 세션 시작 체크리스트

1. `PRD.md` §1.4(구현 단계), §5(보안 키 규칙) 한 번 읽기
2. 이 문서(`PROGRESS.md`) §1~§3 읽기
3. `cd food_search && npm run dev` — 포트 3000이 차있으면 3001로 자동 전환됨, 로그에서 실제 포트 확인
4. `.env.local`에 `KAKAO_REST_API_KEY`만 채워져 있고 나머지 3개는 비어있는 게 정상 상태
5. 코드 변경 후 확인은 `npx tsc --noEmit` 위주로, dev 서버 켜진 채로 `npm run build` 동시 실행 금지
