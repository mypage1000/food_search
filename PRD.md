# PRD — 전국 시티투어 맛집 지도

| 항목 | 내용 |
|---|---|
| 문서 버전 | **v0.3 (1차/2차 구현 단계 분리)** |
| 최초 작성 | 2026-08-20 |
| 최종 수정 | 2026-08-20 |
| 프로젝트명 | city-tour-food-map |
| 로컬 경로 | `C:\Users\223\Desktop\food_search` |
| 상태 | 요건 정의 완료, **1차 구현 착수** (지역·카테고리 필터 + 리스트 출력. 지도·시티투어는 2차로 보류) |
| 관련 문서 | `PROGRESS.md` *(작업 현황·인수인계)*, `DESIGN.md` *(미작성)*, `README.md` *(미작성)* |
| 변경 이력 | v0.1 최초 작성 → v0.2 지역 선택을 시/도 → 구·군 2단으로 변경 → **v0.3 1차 구현 범위를 지역·카테고리 필터+리스트로 한정, 지도·시티투어(F-01·F-02·F-05·F-06)는 2차로 분리** |

> 이 문서는 **무엇을 왜 만드는가**를 다룹니다.
> 색상·타이포·간격 같은 **어떻게 보이는가**는 `DESIGN.md`로 분리합니다.

> ⚠️ **이 프로젝트는 앞선 두 프로젝트와 결정적으로 다른 점이 하나 있습니다.**
> 미세먼지·뉴스 프로젝트에서는 "`NEXT_PUBLIC_`을 절대 붙이지 않는다"가 단순 규칙이었습니다.
> 여기서는 **카카오맵 JS 키만은 브라우저에 노출될 수밖에 없습니다.** 규칙이 "전부 숨긴다"에서
> "**어느 키는 숨기고 어느 키는 노출하되 다른 방법으로 막는다**"로 바뀝니다. §5가 이 문서에서
> 가장 중요한 절입니다.

---

## 0. 요구사항 색인

| ID | 한 줄 요약 | 위치 |
|---|---|---|
| R-01 | 지역 목록(시/도 · 구·군)은 `lib/regions.ts` 한 파일에서만 정의 | §2.1 |
| R-02 | 지역은 `code / label / center / zoom / 소스별 파라미터`를 분리 보관 | §2.1 |
| R-03 | 맛집 데이터 소스는 **어댑터 인터페이스** 뒤에 둔다 (소스 교체 가능) | §2.3 |
| R-04 | 모든 좌표는 **WGS84 경위도**로 정규화한 뒤에만 화면에 넘긴다 | §2.4 |
| R-05 | 좌표 없는 항목은 서버에서 지오코딩, 실패 시 **지도에 찍지 않고 목록에만** 노출 | §2.4 |
| R-06 | 근접 판정은 Haversine 직선거리, 기본 반경 **500 m** | §2.5 |
| R-07 | 음식 카테고리는 원본 분류를 그대로 쓰지 않고 **8종 표준 카테고리로 매핑** | §2.6 |
| R-08 | 미분류 항목은 버리지 않고 "기타"로 남긴다 | §2.6 |
| R-09 | 마커 200개 초과 시 클러스터링 필수 | §3 F-02 |
| R-10 | 정류장 선택 상태는 필터가 아니라 **강조 + 목록 정렬 변경** | §3 F-05 |
| R-11 | 한 소스 실패가 전체 실패가 되지 않는다 (맛집 없어도 노선은 표시) | §4.3 |
| R-12 | 외부 데이터는 **24시간 캐시**, 지오코딩 결과는 파일로 영구 캐시 | §4.4 |
| R-13 | 지도 SDK는 클라이언트에서 **동적 로드**, SSR 시 접근 금지 | §3 F-01 |
| R-14 | 지역 선택은 **시/도 → 구·군 2단 종속 선택**, 구·군에 "전체" 옵션 필수 | §2.1 |
| R-15 | 카테고리 필터는 **지역이 확정된 뒤에만** 활성화된다 | §3 F-04 |
| **S-01~S-10** | **키 노출 방지 규칙 — 이 프로젝트의 핵심** | **§5** |

---

## 1. 개요

### 1.1 한 줄 정의

문화공공데이터광장·공공데이터포털 API로 **시/도 → 구·군으로 좁힌 지역의 맛집을 카카오맵 위에 뿌리고**,
시티투어 노선·정류장을 레이어로 겹쳐 "이 정류장에서 내리면 뭘 먹을 수 있는가"를 보여주는 웹 서비스.

### 1.2 목적

- 서로 다른 두 기관의 API(맛집 / 시티투어)를 **한 지도 위에서 결합**하는 경험을 얻는다.
  앞선 프로젝트들이 단일 API였다면, 여기서는 **데이터 조인**이 주제다.
- **성격이 다른 키 4종**(서버 전용 3종 + 브라우저 노출 1종)을 한 저장소에서 안전하게 다루는 법을 익힌다.
- 좌표계·지오코딩·거리 계산 같은 **지리 데이터의 실무 문제**를 직접 겪는다.

### 1.3 타겟 사용자

- 1차: 시티투어로 처음 가는 도시에서 어디서 뭘 먹을지 정하려는 여행자
- 2차: 포트폴리오로 이 프로젝트를 보게 될 채용 담당자

### 1.4 범위

**구현 단계 (v0.3 추가) — 1차/2차 분리**

| 단계 | 범위 | 상태 |
|---|---|---|
| **1차** | 시/도 → 구·군 지역 선택(F-03) + 음식 카테고리 필터(F-04) + 맛집 목록 패널(F-07) 출력까지 | **진행 중** |
| **2차** | 카카오맵 베이스(F-01) + 마커·클러스터링(F-02) + 시티투어 레이어 토글(F-05) + 정류장 근접 강조(F-06) + 상세 카드(F-08) | 보류 |

> 1차는 지도 없이 **필터 → 목록**만으로 끝까지 동작하는 것이 목표다. 지도·시티투어가 이 프로젝트의 정체성이긴 하지만(§1.2),
> 그 전에 지역·카테고리 조합으로 맛집 데이터가 정확히 걸러져 나오는지부터 검증한다.
>
> **키 요구사항이 줄어든다** — 1차에는 `CULTURE_API_KEY`(맛집 데이터) 하나만 있으면 된다.
> `NEXT_PUBLIC_KAKAO_MAP_JS_KEY`(지도)·`DATA_GO_KR_SERVICE_KEY`(시티투어)·`KAKAO_REST_API_KEY`(지오코딩)는
> 2차 착수 시점에 발급받아도 된다.

**포함 (v1)**

- **시/도 → 구·군 2단 지역 선택** (구·군에 "전체" 옵션)
- 카카오맵 + 맛집 마커 (클러스터링)
- 음식 종류 필터 (8종 카테고리 칩)
- 시티투어 노선·정류장 레이어 **토글**
- 정류장 선택 시 반경 내 맛집 강조 + 거리순 정렬
- 맛집 목록 패널 + 상세 카드
- 상태 화면(로딩·부분결측·에러), 반응형, 출처 표기
- Vercel 배포

**제외 (v1에서 하지 않음)**

| 제외 항목 | 사유 |
|---|---|
| 하루 코스 자동 추천 | 추천 로직·이동시간 계산이 별도 주제. v2 |
| 길찾기 / 경로 안내 | 카카오 모빌리티 API는 별도 신청·과금 체계 |
| 리뷰·평점·정렬 | 공공데이터에 평점이 없다. 크롤링은 범위 밖 |
| 회원가입 / 즐겨찾기 | 사용자별 저장 데이터 없음 |
| 실시간 버스 위치 | 시티투어 버스는 실시간 위치 API가 없다 |
| 다국어 | v1은 한국어 고정 |

### 1.5 기술 스택 (확정)

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js 16 (App Router) | 서버 라우트로 서비스키를 감춤. 앞선 두 프로젝트와 동일 |
| 언어 | TypeScript | 좌표·결측·카테고리 매핑을 타입으로 고정 |
| UI | React 19 | 지도·필터·목록이 하나의 상태를 공유 |
| 스타일 | 순수 CSS (`app/globals.css`) | 기존 프로젝트와 동일. 라이트 테마 고정 |
| 지도 | **카카오맵 JS SDK** | 국내 상호·건물 표기 품질. 마커·클러스터러 기본 제공 |
| 지오코딩 | 카카오 로컬 REST API | 주소만 있는 항목의 좌표 보정 (§2.4) |
| 상태관리 | React `useState` | 화면 하나 규모 |
| 배포 | Vercel | 환경변수는 대시보드 등록 |

> **지도를 인라인 SVG로 그리지 않는 이유** — 미세먼지 프로젝트는 도형 16개짜리 개념도라 SVG로 충분했지만,
> 여기서는 실제 도로 위 좌표에 수백 개 마커를 찍고 확대·이동해야 합니다. 타일 지도가 필요한 첫 프로젝트입니다.

---

## 2. 핵심 데이터 정의

### 2.1 지역 단위 — 시/도 → 구·군 2단 선택

지역은 **두 단계로 좁혀서** 선택합니다. 전국 맛집을 한 화면에 뿌리면 마커가 수만 개가 되고,
그 상태의 지도는 정보가 아니라 얼룩입니다. 사용자가 스스로 범위를 좁히게 만드는 것이 첫 화면의 일입니다.

| 단계 | 선택 대상 | 비고 |
|---|---|---|
| 1단계 | **시/도** (16개) | 필수. 미선택 시 안내 화면 |
| 2단계 | **구·군** | 1단계 선택에 따라 목록이 바뀜. 맨 앞에 **"전체"** |
| 3단계 | **음식 카테고리** (§2.6) | **지역이 확정된 뒤에만** 활성화 (R-15) |

**시/도는 16개 기준입니다.** 2026-07-01 전남광주통합특별시 출범으로 전라남도와 광주광역시가
폐지되었습니다. 미세먼지 프로젝트와 같은 기준을 씁니다.

**R-14 (필수)** — 지역 선택은 **종속 선택(cascading)** 이다.

- 시/도를 바꾸면 구·군 선택과 카테고리 선택은 **초기화**된다.
- 구·군 목록에는 항상 **"전체"** 를 맨 앞에 둔다.
- 구·군을 고르지 않아도(= "전체") 지도는 동작한다. 시/도만 고른 상태가 유효한 상태다.

> **"전체"가 반드시 필요한 이유** — 없으면 부산 전체를 보고 싶은 사용자가 16개 구를 하나씩
> 눌러야 합니다. 다만 서울 "전체"는 마커가 수천 개가 되므로, "전체" 선택 시에는
> 클러스터링(R-09)이 무조건 켜집니다.

**R-01 (필수)** — 지역 목록은 `lib/regions.ts` **한 파일에서만** 정의한다.
지역 선택 UI·지도 초기 좌표·API 파라미터가 모두 이 트리를 참조한다.

**R-02 (필수)** — 각 지역은 다음 값을 분리해 갖는다. 화면 표기와 API 파라미터를 섞지 않는다.

```ts
type Sido = {
  code: string;        // 행정표준코드 앞 2자리. 예: '47'
  label: string;       // '경상북도'
  short: string;       // '경북' — 칩·배지용 축약 표기
  center: { lat: number; lng: number };  // 구·군 "전체" 선택 시 지도 중심
  zoom: number;
  sigungu: Sigungu[];
};

type Sigungu = {
  code: string;        // 행정표준코드 5자리. 예: '47130'
  label: string;       // '경주시'
  center: { lat: number; lng: number };  // WGS84
  zoom: number;
  hasCityTour: boolean;                  // 시티투어 레이어 제공 여부
  foodParams: Record<string, string>;    // 맛집 API 지역 파라미터
  tourParams?: Record<string, string>;   // 시티투어 API 지역 파라미터
};
```

> `foodParams` / `tourParams`를 **자유 형태 객체**로 둔 이유는, 두 API의 지역 지정 방식이
> 서로 다를 가능성이 매우 높기 때문입니다(한쪽은 시도명 문자열, 다른 쪽은 법정동 코드일 수 있음).
> 실측 전에 타입을 좁히면 STEP 0에서 반드시 고치게 됩니다.

**구·군 목록의 출처** — 행정안전부 **행정표준코드**를 기준으로 정적 파일에 담는다(약 230개).
API로 매번 받아오지 않는다. 거의 변하지 않는 데이터를 런타임 의존성으로 만들 이유가 없다.

### 2.1.1 v1 데이터 확보 대상 — 6개 도시

지역 선택 UI는 **전국을 노출**하되, 맛집 데이터 검증과 시티투어 레이어는 아래 6개부터 확보합니다.

| # | 시/도 | 구·군 | 선정 사유 |
|---|---|---|---|
| 1 | 서울특별시 | 전체 | 노선·정류장 수가 가장 많음. 데이터 밀도 상한 테스트 |
| 2 | 부산광역시 | 전체 | 다중 노선(레드/블루/그린) 운영. 노선 구분 UI 검증 |
| 3 | 경상북도 | 경주시 | 관광지 밀집형. 정류장 간 거리가 짧은 경우 검증 |
| 4 | 전라북도 | 전주시 | 음식 콘텐츠가 강한 도시 |
| 5 | 전남광주특별시 | 여수시 | 해안형 노선. 반경 안에 바다가 들어오는 경우 검증 |
| 6 | 강원특별자치도 | 강릉시 | 데이터가 얇을 때 화면이 어떻게 보이는지 검증 |

**시티투어가 없는 지역을 고른 경우** — 맛집 지도는 **정상 동작**하고, 시티투어 레이어 토글만
비활성화된다(F-05). 선택 목록에서 `hasCityTour: true`인 지역에는 배지를 붙여 미리 알린다.

> 시티투어가 없는 지역을 아예 고르지 못하게 막는 것도 방법이지만, 그러면 "왜 우리 동네는 회색이지"
> 라는 질문이 생깁니다. 고를 수 있게 하되 무엇이 없는지 미리 알려주는 편이 낫습니다.

### 2.2 시티투어 데이터

| 항목 | 내용 |
|---|---|
| 출처 | 공공데이터포털 **전국시티투어정보표준데이터** |
| 단위 | 코스(노선) → 정류장 목록 |
| 우리가 쓰는 값 | 코스명, 운영기관, 운행요일/시간, 요금, 정류장명, 정류장 좌표, 정류장 순번 |

**정규화 결과 타입**

```ts
type TourCourse = {
  id: string;
  sigunguCode: string;
  name: string;          // '부산 레드라인'
  operator: string;
  schedule?: string;     // 운행 요일·배차. 없으면 미표시
  fare?: string;
  stops: TourStop[];     // 순번 오름차순
};

type TourStop = {
  id: string;
  name: string;
  order: number;
  lat: number | null;    // WGS84. 보정 실패 시 null
  lng: number | null;
};
```

> **표준데이터가 정류장 좌표를 주지 않을 수 있습니다** (RSK-02). 코스 단위 정보만 있고
> 정류장이 텍스트 한 줄로 뭉쳐 있는 형태일 가능성이 실제로 있습니다.
> 그 경우 §2.4의 지오코딩으로 정류장명을 좌표로 바꿉니다.

### 2.3 맛집 데이터 — 어댑터 뒤에 둔다

**R-03 (필수)** — 맛집 데이터 소스는 `lib/sources/food/` 아래 **어댑터 인터페이스** 뒤에 둔다.
화면과 서버 로직은 어댑터가 돌려주는 `Restaurant[]`만 알고, 어느 기관의 API인지는 모른다.

```ts
type RegionQuery = {
  sido: Sido;
  sigungu: Sigungu | null;   // null = "전체"
};

interface FoodSource {
  id: string;                                     // 'culture' | 'tourapi' | ...
  label: string;                                  // 출처 표기에 그대로 쓰임
  fetchByRegion(q: RegionQuery): Promise<RawItem[]>;
  normalize(raw: RawItem): Restaurant | null;     // 필수값 없으면 null → 버림
}
```

> **왜 이렇게까지 하는가** — 문화공공데이터광장의 맛집 성격 데이터셋이 실제로 어떤 항목을
> 어느 커버리지로 주는지는 **활용신청 후 응답을 받아봐야만 알 수 있습니다.** 조사 단계에서
> 포털이 자동 조회를 차단(robots.txt)해 목록을 확인하지 못했습니다.
> 소스가 화면 코드에 직접 박혀 있으면, 커버리지가 기대에 못 미쳤을 때 프로젝트 전체를 다시 짜야 합니다.
> 어댑터가 있으면 **파일 하나를 추가하는 것으로** 소스를 바꾸거나 더할 수 있습니다.

**v1 소스 우선순위**

| 순위 | 소스 | 상태 |
|---|---|---|
| 1 | 문화공공데이터광장 맞춤형 API (사용자가 직접 발급) | **채택** |
| 예비 | 한국관광공사 TourAPI 음식점 (`contentTypeId=39`) | 1순위 커버리지가 부족할 때만 어댑터 추가 |

**정규화 결과 타입**

```ts
type Restaurant = {
  id: string;
  sidoCode: string;
  sigunguCode: string | null;   // 원본이 구·군을 안 주면 null
  name: string;             // 필수. 없으면 버림
  category: FoodCategory;   // §2.6의 8종
  rawCategory?: string;     // 원본 분류. 매핑 검수용으로 보존
  address: string;
  lat: number | null;       // WGS84
  lng: number | null;
  tel?: string;
  hours?: string;
  menu?: string;
  imageUrl?: string;
  sourceId: string;         // 출처 표기용
  geocoded: boolean;        // 좌표를 우리가 만들어냈는지
};
```

### 2.4 좌표계 — 조용히 망가지는 지점

**R-04 (필수)** — 모든 좌표는 **WGS84 경위도(EPSG:4326)** 로 정규화한 뒤에만 화면에 넘긴다.
어댑터의 `normalize()` 안에서 변환을 끝내고, 그 바깥에서는 좌표계를 신경 쓰지 않는다.

국내 공공데이터는 좌표계가 제각각입니다. 실측에서 확인할 것:

| 좌표계 | 값의 생김새 | 처리 |
|---|---|---|
| WGS84 (EPSG:4326) | 위도 33~39, 경도 124~132 | 그대로 사용 |
| GRS80 중부원점 (EPSG:5186) | 수십만 단위 정수 | `proj4`로 변환 |
| UTM-K (EPSG:5179) | 백만 단위 정수 | `proj4`로 변환 |

> **검증 규칙** — 정규화 직후 `33 ≤ lat ≤ 39 && 124 ≤ lng ≤ 132`를 만족하지 않으면
> 좌표를 `null`로 떨어뜨리고 로그를 남깁니다. 좌표계를 잘못 읽으면 마커가 아프리카 앞바다에
> 찍히는데, 이건 에러가 아니라 **조용한 오작동**이라 검사를 걸어두지 않으면 못 잡습니다.
>
> **위경도 순서도 함께 의심합니다.** 항목명이 `x`/`y`인 경우 `x=경도`가 보통이지만 반대인
> 데이터도 있습니다. 값의 범위로 판정하면 순서 착오까지 같이 걸러집니다.

**R-05 (필수)** — 좌표가 없거나 검증에 실패한 항목은 **서버에서 카카오 로컬 REST API로 지오코딩**한다.
지오코딩도 실패하면 **버리지 않고** `lat/lng = null`로 두어 **목록에는 남기되 지도에는 찍지 않는다.**

- 지오코딩은 **서버에서만** 수행한다 (REST 키가 브라우저에 나가면 안 됨 — S-05).
- 결과는 `data/geocode-cache.json`에 **주소 → 좌표**로 영구 저장한다 (R-12).
  같은 주소를 두 번 조회하지 않기 위해서다.
- 목록 항목에는 "지도에 표시할 수 없음" 캡션을 붙인다.

### 2.5 근접 판정 — "정류장 근처"의 정의

**R-06 (필수)** — 정류장과 맛집의 거리는 **Haversine 직선거리**로 계산하고, 기본 반경은 **500 m**로 한다.

| 반경 | 도보 환산 | 비고 |
|---|---|---|
| 300 m | 약 4분 | 정류장 바로 앞 |
| **500 m** | **약 7분** | **기본값** |
| 1,000 m | 약 14분 | 넓게 보기 |

> **직선거리를 쓰는 이유** — 도보 실거리를 구하려면 경로 API가 필요하고, 그건 v1 범위 밖입니다(§1.4).
> 대신 화면에 **"직선거리 기준"**을 명시해 실제 도보 거리와 다를 수 있음을 알립니다.
> 특히 여수처럼 사이에 물이나 언덕이 있는 도시에서는 차이가 큽니다.

- 거리 표기는 1,000 m 미만이면 `m`(10 m 단위 반올림), 이상이면 `km`(소수 첫째 자리).
- 반경 선택은 3개 버튼 토글. 기본 500 m.

### 2.6 음식 카테고리 — 8종으로 매핑

**R-07 (필수)** — 원본 분류 문자열을 그대로 필터에 쓰지 않는다. `lib/categories.ts`에서
**8종 표준 카테고리로 매핑**한 뒤 사용한다.

| 카테고리 | 매핑 대상 예시 |
|---|---|
| 한식 | 한식, 백반, 국밥, 한정식, 향토음식 |
| 중식 | 중식, 중화요리 |
| 일식 | 일식, 초밥, 돈까스 |
| 양식 | 양식, 이탈리안, 스테이크 |
| 분식 | 분식, 김밥, 떡볶이 |
| 카페·디저트 | 카페, 제과, 베이커리, 찻집 |
| 해산물 | 회, 횟집, 조개구이, 해물탕 |
| 기타 | 위에 걸리지 않는 모든 것 |

> **왜 매핑이 필요한가** — 공공데이터의 분류 문자열은 기관마다 다르고, 같은 기관 안에서도
> "한식"·"한식류"·"한식(일반)"이 섞여 있습니다. 원본을 그대로 칩으로 만들면 칩이 40개가 되고,
> 사용자는 "한식"과 "한식류"의 차이를 물어보게 됩니다.

**R-08 (필수)** — 어느 카테고리에도 매핑되지 않는 항목을 **버리지 않는다.** "기타"로 남기고
`rawCategory`를 보존한다. 개발 중 "기타" 비율이 20%를 넘으면 매핑 표를 보강한다.

- **해산물을 한식에서 분리한 이유**: v1 확보 대상 6곳 중 부산·여수·강릉이 해안 도시입니다.
  이 도시들에서 해산물이 한식에 묻히면 필터가 제 역할을 못 합니다.

---

## 3. 기능 요구사항

| ID | 기능 | 우선순위 |
|---|---|---|
| F-01 | 카카오맵 베이스 | P0 |
| F-02 | 맛집 마커 + 클러스터링 | P0 |
| F-03 | 지역 선택 (시/도 → 구·군) | P0 |
| F-04 | 음식 종류 필터 | P0 |
| F-05 | 시티투어 레이어 토글 | P0 |
| F-06 | 정류장 근처 맛집 강조 | P0 |
| F-07 | 맛집 목록 패널 | P0 |
| F-08 | 맛집 상세 카드 | P0 |
| F-09 | 상태 화면 | P0 |
| F-10 | 반응형 | P0 |
| F-11 | 출처 표기 | P0 |

### F-01. 카카오맵 베이스 · P0

| 항목 | 내용 |
|---|---|
| 설명 | 화면 대부분을 차지하는 지도. 지역 선택 시 해당 지역 중심으로 이동 |
| 초기 상태 | 지역 미선택. 전국이 보이는 축척 + "시/도를 선택해 주세요" 안내 (F-09 초기 상태) |
| 지역 확정 시 | 구·군을 골랐으면 구·군 `center/zoom`, "전체"면 시/도 `center/zoom`으로 이동 |
| 컨트롤 | 확대/축소, 현재 도시로 되돌아가기 버튼 |
| 제외 | 로드뷰, 지적편집도, 교통정보 — v1에서 쓰지 않음 |

**R-13 (필수)** — 카카오맵 SDK는 **클라이언트에서 동적 로드**한다. SSR 중에는 `window.kakao`에
접근하지 않는다.

```tsx
'use client';
// SDK 스크립트를 autoload=false로 삽입 → onload에서 kakao.maps.load(cb)
```

> Next.js App Router는 기본이 서버 컴포넌트라, 지도 컴포넌트에 `'use client'`를 빠뜨리면
> `window is not defined`로 빌드가 깨집니다. 지도 관련 코드는 전부 클라이언트 경계 안쪽에 둡니다.

### F-02. 맛집 마커 + 클러스터링 · P0

| 항목 | 내용 |
|---|---|
| 마커 | 카테고리별로 구분되는 마커. **색만으로 구분하지 않고 아이콘 모양도 다르게** 한다 |
| 선택 상태 | 목록에서 선택된 항목은 마커가 커지고 위로 올라옴(zIndex) |
| 좌표 없음 | 지도에 찍지 않음 (R-05). 목록에만 존재 |

**R-09 (필수)** — 화면에 그릴 마커가 **200개를 넘으면 클러스터링**한다.
카카오맵 `MarkerClusterer`를 사용하고, 클러스터를 누르면 확대한다.

> 서울처럼 데이터가 두터운 도시는 마커가 1,000개를 넘길 수 있습니다. 개별 마커를 전부 그리면
> 모바일에서 지도 조작이 눈에 띄게 버벅입니다.

### F-03. 지역 선택 (시/도 → 구·군) · P0

**화면의 시작점입니다.** 여기서 범위가 정해져야 나머지가 의미를 갖습니다.

| 항목 | 내용 |
|---|---|
| 위치 | 헤더 또는 지도 상단 고정. 항상 보이는 자리 |
| 1단계 | **시/도 16개.** 데스크톱은 칩(또는 셀렉트), 모바일은 셀렉트 |
| 2단계 | **구·군.** 시/도 선택 전에는 **비활성**. 선택 후 목록이 채워지며 맨 앞은 **"전체"** |
| 기본값 | 둘 다 미선택. 초기 화면은 안내 상태 (F-09) |
| 종속 초기화 | 시/도를 바꾸면 구·군·카테고리·정류장 선택이 모두 초기화 (R-14) |
| 시티투어 배지 | `hasCityTour: true`인 항목에 배지 표시 |
| 개수 표기 | 데이터를 받은 뒤 "총 128곳"을 지역 표시 옆에 병기 |
| 되돌리기 | 선택한 지역을 보여주는 브레드크럼(`경상북도 › 경주시`)에서 각 단계로 복귀 |
| 접근성 | 셀렉트는 `<label>` 연결, 칩은 `aria-pressed`. 높이 44px 이상 |
| URL 동기화 | `?sido=47&sigungu=47130` 형태로 반영해 새로고침·공유가 가능하게 한다 |

> **왜 시/도를 먼저 강제하는가** — 지역을 안 고른 상태에서 전국 맛집을 미리 보여주면
> 첫 요청부터 수만 건을 받아야 하고, 사용자는 자기 지역을 찾으려고 지도를 한참 끌어야 합니다.
> "고르기 전에는 아무것도 안 보여준다"가 불친절해 보이지만, 이 데이터 규모에서는 정직한 설계입니다.

### F-04. 음식 종류 필터 · P0

| 항목 | 내용 |
|---|---|
| 설명 | §2.6의 8종 카테고리 칩. **다중 선택** |
| **활성 조건** | **시/도가 확정된 뒤에만 활성화된다 (R-15).** 그 전에는 비활성 + "지역을 먼저 선택해 주세요" |
| 기본값 | 전체 선택 해제 = 전부 표시 |
| 동작 | 지도 마커와 목록 패널에 **동시에** 적용. **추가 API 호출 없음** |
| 표시 | 각 칩에 현재 지역 기준 개수 병기 (예: `한식 128`) |
| 0건 카테고리 | 개수가 0인 칩은 **숨기지 않고 비활성**으로 둔다. 있다가 사라지면 더 혼란스럽다 |
| 결과 0건 | 목록에 "조건에 맞는 맛집이 없습니다" + 필터 초기화 버튼 |
| 지역 변경 시 | 카테고리 선택은 초기화된다 (R-14). 개수가 지역마다 다르므로 유지할 근거가 없다 |
| URL 동기화 | `&cat=한식,해산물` 형태로 반영 |

**R-15 (필수)** — 카테고리 필터는 지역이 확정된 뒤에만 활성화된다.

> **순서를 강제하는 이유** — "전국 한식"은 우리가 답할 수 있는 질문이 아닙니다(§F-03의 규모 문제).
> 필터는 이미 좁혀진 집합을 다시 좁히는 도구이지, 그 자체로 검색이 아닙니다.
> 비활성 칩을 회색으로 두되 **숨기지는 않습니다** — 다음에 무엇을 할 수 있는지 미리 보여주는 편이
> 낫고, 갑자기 새 UI가 나타나는 것보다 예측 가능합니다.

### F-05. 시티투어 레이어 토글 · P0

| 항목 | 내용 |
|---|---|
| 설명 | 지도 위에 시티투어 정류장 마커와 노선 폴리라인을 **켜고 끄는** 스위치 |
| 기본값 | **켜짐**. 이 사이트의 정체성이므로 처음부터 보이게 한다 |
| 노선이 여러 개 | 부산처럼 다중 노선이면 노선별 색 구분 + 노선 선택 드롭다운 |
| 폴리라인 | 정류장 순번을 이어 그린다. **실제 도로 경로가 아니라 정류장 연결선**임을 캡션에 명시 |
| 정류장 마커 | 맛집 마커와 **형태가 확연히 다르게** 한다 (버스 아이콘 등). 순번 숫자 표기 |
| 좌표 없는 정류장 | 폴리라인에서 건너뛴다. 코스 정보 패널에는 이름을 남긴다 |

> **폴리라인이 실제 노선이 아님을 반드시 밝히는 이유** — 표준데이터는 정류장 목록을 줄 뿐
> 주행 경로(shape)를 주지 않습니다. 직선으로 이으면 바다나 산을 관통하는 선이 그려집니다.
> 이걸 실제 노선으로 오해하면 사용자가 잘못된 판단을 합니다.

### F-06. 정류장 근처 맛집 강조 · P0

**두 데이터를 연결하는 이 프로젝트의 핵심 기능입니다.**

| 항목 | 내용 |
|---|---|
| 트리거 | 지도의 정류장 마커 클릭, 또는 코스 패널의 정류장 항목 클릭 |
| 지도 | 정류장 중심 반경 원을 그림. 반경 밖 맛집 마커는 **투명도를 낮춰 흐리게** |
| 목록 | 반경 안 맛집만 남기고 **거리 오름차순** 정렬. 각 항목에 거리 배지(`320 m`) |
| 헤더 | "○○정류장 반경 500 m · 12곳" + 반경 토글(300/500/1000) + **선택 해제** 버튼 |
| 0건 | "반경 안에 맛집 데이터가 없습니다" + 반경 넓히기 제안 |
| 필터와의 관계 | 카테고리 필터와 **AND**로 결합한다 |

**R-10 (필수)** — 정류장 선택은 반경 밖 마커를 **지우지 않고 흐리게** 한다.

> 마커를 지우면 지도가 갑자기 비어 사용자가 "고장났나" 하고 되돌아갑니다. 흐리게 두면
> 주변 맥락을 유지한 채 초점만 이동합니다. 목록 패널은 반대로 반경 안만 남깁니다 —
> 목록은 "지금 볼 것"의 자리이기 때문입니다.

### F-07. 맛집 목록 패널 · P0

| 항목 | 내용 |
|---|---|
| 위치 | 데스크톱은 지도 우측 고정 패널, 그 미만은 지도 아래 (§F-10) |
| 한 줄 구성 | `썸네일 · 이름 · 카테고리 배지 · (정류장 선택 시) 거리 배지` |
| 정렬 | 기본은 이름 가나다순. 정류장 선택 중이면 **거리순** |
| 상호작용 | 항목 hover → 해당 마커 강조 / 클릭 → 상세 카드(F-08) + 지도 이동 |
| 개수 표기 | 패널 상단에 "총 128곳" (필터 적용 후 기준) |
| 가상 스크롤 | 항목 300개 초과 시 검토. v1은 우선 페이지네이션(더 보기)으로 처리 |

### F-08. 맛집 상세 카드 · P0

| 항목 | 내용 |
|---|---|
| 구성 | 사진 · 이름 · 카테고리 배지 · 주소 · 전화 · 영업시간 · 대표메뉴 · 출처 |
| 없는 값 | **"정보 없음"을 나열하지 않는다.** 값이 없는 항목은 줄 자체를 그린다 |
| 좌표 없음 | "지도에 표시할 수 없는 항목입니다" 캡션 |
| 지오코딩됨 | `geocoded: true`이면 "주소 기반 추정 위치" 캡션. 정확도를 속이지 않는다 |
| 카카오맵 열기 | 카카오맵 웹으로 이동하는 외부 링크 (길찾기는 그쪽에 맡긴다) |
| 사진 없음 | 카테고리 아이콘 플레이스홀더. 깨진 이미지 아이콘을 보이지 않는다 |

### F-09. 상태 화면 · P0

| 상태 | 조건 | 화면 |
|---|---|---|
| **초기(미선택)** | 시/도를 아직 안 고름 | 전국 축척 지도 + "시/도를 선택해 주세요" 안내. 구·군·카테고리 비활성 |
| **로딩** | 지역 데이터 요청 중 | 지도는 그대로 두고 목록 패널만 스켈레톤. 칩 비활성 |
| **맛집만 실패** | 시티투어는 성공 | 노선·정류장은 표시 + "맛집 정보를 불러오지 못했습니다" 배너 |
| **시티투어만 실패** | 맛집은 성공 | 맛집 지도 정상 + 레이어 토글 비활성 + 사유 툴팁 |
| **전체 실패** | 둘 다 실패 | 에러 화면 + 원인별 문구(§4.3) + **다시 시도** 버튼 |
| **지도 로드 실패** | SDK 스크립트 실패 | 지도 자리에 안내 + **목록 패널은 그대로 동작** |

> **지도 로드 실패 시에도 목록이 살아 있어야 합니다.** 카카오 JS 키의 도메인 등록이
> 잘못되면 지도만 통째로 안 뜨는데(S-08), 그때 사이트 전체가 백지가 되면 원인 파악조차 어렵습니다.

### F-10. 반응형 · P0

브레이크포인트는 **640px / 1024px 두 값만** 쓴다 (미세먼지 프로젝트와 동일 규칙).

| 요소 | 모바일 (~640) | 태블릿 (641~1024) | 데스크톱 (1025~) |
|---|---|---|---|
| 전체 흐름 | 지도(상단 고정 높이) → 바텀시트 목록 | 지도 → 아래 목록 2열 그리드 | 좌 지도 / 우 목록 2단 |
| 지도 높이 | 55vh | 60vh | 전체 높이 (`100dvh - 헤더`) |
| 목록 패널 | **드래그 가능한 바텀시트**(접힘/절반/펼침 3단) | 지도 아래 전체 폭 | 우측 고정, 최소 360px, 자체 스크롤 |
| 지역 선택 | 시/도·구·군 **셀렉트 2개** 세로 배치 | 셀렉트 2개 가로 배치 | 시/도 칩 1줄 + 구·군 셀렉트 |
| 카테고리 칩 | 1줄 가로 스크롤 | 줄바꿈 | 줄바꿈 |
| 상세 카드 | 전체 화면 오버레이 | 목록 위 확장 | 우측 패널 안에서 전환 |

| 항목 | 규칙 |
|---|---|
| 터치 대상 | 모든 구간 최소 44×44px |
| 마커 터치 | 모바일에서 마커 이미지를 크게 (최소 40px) |
| hover | `@media (hover: hover)`로 감싼다 |
| 본문 글자 | 최소 16px (iOS 자동 확대 방지) |
| 지도 제스처 | 모바일에서 지도가 페이지 세로 스크롤을 가로채지 않게 처리 |

**검증 기준** — 360 / 390 / 768 / 1024 / 1280 / 1440px에서 가로 스크롤 없음.
모바일 바텀시트가 지도 조작을 막지 않음.

### F-11. 출처 표기 · P0

| 항목 | 내용 |
|---|---|
| 맛집 | 어댑터의 `label`을 그대로 노출. 상세 카드와 푸터 양쪽에 |
| 시티투어 | "출처: 공공데이터포털 전국시티투어정보표준데이터" |
| 지도 | 카카오 로고·저작권 표기는 SDK 기본값을 **가리지 않는다** (이용약관 위반) |
| 가공 고지 | 카테고리는 우리가 8종으로 재분류한 값이며, 일부 좌표는 주소 기반 추정임을 명시 |
| 라이선스 | 각 데이터셋의 공공누리 조건을 확인해 푸터에 표기 (STEP 0에서 확인) |

---

## 4. API 연동 명세

### 4.1 외부 API

| # | API | 용도 | 키 |
|---|---|---|---|
| 1 | 문화공공데이터광장 맞춤형 API | 맛집 목록 | `CULTURE_API_KEY` (서버) |
| 2 | 공공데이터포털 전국시티투어정보표준데이터 | 코스·정류장 | `DATA_GO_KR_SERVICE_KEY` (서버) |
| 3 | 카카오맵 JS SDK | 지도 렌더링 | `NEXT_PUBLIC_KAKAO_MAP_JS_KEY` (**브라우저**) |
| 4 | 카카오 로컬 REST API | 주소 → 좌표 | `KAKAO_REST_API_KEY` (서버) |

> **⚠️ STEP 0에서 반드시 실측할 것 (RSK-01)**
> 1. 문화공공데이터광장에 **전국 단위 맛집/음식점 성격의 맞춤형 API가 실제로 있는가**
> 2. 있다면 **지역 지정 파라미터**가 무엇인가 (시도명? 지역코드? 검색어?)
> 3. 응답에 **좌표가 포함되는가**, 좌표계는 무엇인가
> 4. 대상 6개 도시의 **건수가 화면을 채울 만한가** (도시당 최소 30곳을 기준으로 삼는다)
> 5. 시티투어 표준데이터가 **정류장 단위 좌표**를 주는가, 코스 단위 텍스트인가
>
> 위 5개를 확인하기 전에는 UI 구현에 들어가지 않습니다. 하나라도 기대와 다르면
> §2.5(근접 판정)나 F-06(핵심 기능)이 성립하지 않을 수 있기 때문입니다.
> **커버리지가 부족하면 R-03의 어댑터에 TourAPI 음식점 소스를 추가합니다.**

### 4.2 내부 API (프록시)

브라우저가 직접 호출하면 서비스키가 노출되므로 서버가 중계합니다.

```
GET /api/region?sido=47&sigungu=47130
```

| 파라미터 | 필수 | 설명 |
|---|---|---|
| `sido` | ✅ | 행정표준코드 앞 2자리. 없으면 `400 MISSING_REGION` |
| `sigungu` | ❌ | 5자리. **생략 = "전체"** |

해당 지역의 **맛집 + 시티투어를 한 번에** 반환합니다. 화면 전체가 같은 데이터를 쓰므로
엔드포인트를 나눌 이유가 없습니다. 카테고리는 **서버 파라미터가 아닙니다** —
필터는 클라이언트에서 적용하므로 칩을 눌러도 추가 호출이 없습니다(F-04).

서버 처리 순서:

1. 캐시 확인 → 유효하면 즉시 반환
2. 맛집 어댑터 호출 + 시티투어 호출 (**병렬**)
3. 각각 정규화 → 좌표 검증 (§2.4)
4. 좌표 없는 항목 지오코딩 (지오코딩 캐시 우선 조회)
5. 카테고리 매핑 (§2.6)
6. 캐시 저장 후 반환

**성공 응답**

```json
{
  "sido": { "code": "47", "label": "경상북도" },
  "sigungu": { "code": "47130", "label": "경주시" },
  "hasCityTour": true,
  "updatedAt": "2026-08-20T02:00:00.000Z",
  "restaurants": [
    {
      "id": "culture-10293",
      "name": "황남맷돌순두부",
      "sidoCode": "47", "sigunguCode": "47130",
      "category": "한식",
      "rawCategory": "한식류",
      "address": "경북 경주시 ...",
      "lat": 35.8391, "lng": 129.2115,
      "tel": "054-...", "hours": "09:00-20:00",
      "imageUrl": "https://...",
      "sourceId": "culture",
      "geocoded": false
    }
  ],
  "courses": [
    {
      "id": "gyeongju-main",
      "sigunguCode": "47130",
      "name": "경주 시티투어 A코스",
      "operator": "경주시",
      "stops": [
        { "id": "s1", "name": "경주역", "order": 1, "lat": 35.8419, "lng": 129.2264 }
      ]
    }
  ],
  "partial": { "restaurants": true, "courses": true },
  "counts": { "total": 128, "geocoded": 14, "noCoords": 3 }
}
```

### 4.3 에러 처리

| code | HTTP | 상황 | 사용자에게 보이는 문구 |
|---|---|---|---|
| `MISSING_REGION` | 400 | `sido` 누락 | (화면에 노출되지 않음 — F-03이 먼저 막는다) |
| `UNKNOWN_REGION` | 404 | 코드가 `regions.ts`에 없음 | 알 수 없는 지역입니다. |
| `MISSING_KEY` | 500 | 환경변수 미설정 | 서버에 API 키가 설정되어 있지 않습니다. |
| `INVALID_KEY` | 401 | 인증키 오류 / 미등록 | API 인증에 실패했습니다. 서비스키를 확인해 주세요. |
| `RATE_LIMITED` | 429 | 일 한도 초과 | 오늘의 API 호출 한도를 초과했습니다. 잠시 후 다시 시도해 주세요. |
| `UPSTREAM_ERROR` | 502 | 원본 5xx / 파싱 실패 | 데이터 서버에 일시적인 문제가 발생했습니다. |
| `NETWORK_ERROR` | 503 | fetch 실패 | 서버에 연결하지 못했습니다. |
| `ALL_SOURCES_FAILED` | 502 | 두 소스 모두 실패 | 정보를 불러오지 못했습니다. |

**R-11 (필수)** — **한 소스의 실패가 전체 실패가 되지 않는다.**
맛집이 실패해도 시티투어 노선은 그리고, 그 반대도 마찬가지다.
`partial` 플래그로 어느 쪽이 성공했는지 전달해 F-09의 부분 실패 화면을 띄운다.

> 원본 서버의 에러 본문은 그대로 전달하지 않고 서버 로그에만 기록한다. **키 유출 방지** (S-09).

### 4.4 캐시 전략

**R-12 (필수)**

| 데이터 | 주기 | 방식 |
|---|---|---|
| 맛집 목록 | **24시간** | Next.js Data Cache — `fetch(url, { next: { revalidate: 86400 } })` |
| 시티투어 코스 | **24시간** | 동일 |
| 지오코딩 결과 | **영구** | `data/geocode-cache.json` (저장소에 커밋) |

> **왜 24시간인가** — 미세먼지는 시간 단위로 변하는 데이터라 1시간이었지만, 음식점 목록과
> 버스 노선은 **거의 변하지 않습니다.** 짧게 잡을 이유가 없고, 호출 한도만 소모합니다.
>
> **Vercel 서버리스 주의** — 프로세스 메모리 캐시는 인스턴스 간에 공유되지 않습니다.
> 반드시 Data Cache 또는 `unstable_cache`를 씁니다. `cache: 'no-store'`는 쓰지 않습니다.

> **지오코딩 캐시를 저장소에 커밋하는 이유** — 서버리스에서는 파일 쓰기가 인스턴스 간에 공유되지
> 않고 배포마다 사라집니다. 개발 중 로컬에서 캐시를 채워 커밋해 두면, 배포 환경에서는 **읽기 전용**으로
> 재사용됩니다. 이 파일에는 공개 주소와 좌표만 들어가므로 커밋해도 안전합니다.

**캐시 키는 `sido + sigungu` 조합**이다. "전체"와 개별 구·군은 서로 다른 캐시 항목이다.

**예상 호출량** — v1 확보 대상 6곳 × 소스 2종 = 12회/일 (캐시 히트 시 0회).
표준데이터 페이지네이션을 감안해도 여유가 큽니다. 지오코딩은 캐시 이후 0회에 수렴합니다.

---

## 5. 보안 요구사항 — 키를 두 등급으로 나눈다

**이 프로젝트에는 성격이 다른 키가 4개 있습니다.** 하나로 뭉뚱그려 다루면 반드시 사고가 납니다.

### 5.1 키 분류

| 키 | 등급 | 브라우저 노출 | 방어 수단 |
|---|---|---|---|
| `CULTURE_API_KEY` | **A · 절대 비공개** | ❌ | 서버 라우트에서만 사용 |
| `DATA_GO_KR_SERVICE_KEY` | **A · 절대 비공개** | ❌ | 서버 라우트에서만 사용 |
| `KAKAO_REST_API_KEY` | **A · 절대 비공개** | ❌ | 서버 라우트에서만 사용 |
| `NEXT_PUBLIC_KAKAO_MAP_JS_KEY` | **B · 노출 전제** | ✅ (불가피) | **카카오 개발자 콘솔 도메인 등록** |

**S-01** — A등급 키는 소스코드에 직접 쓰지 않고 환경변수에서만 읽는다.

**S-02 (필수)** — A등급 키에는 **`NEXT_PUBLIC_` 접두사를 절대 붙이지 않는다.**
붙이는 순간 번들에 그대로 박혀 브라우저에서 읽힌다.

**S-03 (필수)** — A등급 키는 **`app/api/` 아래 서버 코드에서만** 참조한다.
`'use client'` 파일에서 `process.env`로 A등급 키를 읽는 코드는 리뷰에서 즉시 반려한다.

**S-04 (필수)** — 카카오 **JS 키와 REST 키는 서로 다른 키다.** 혼동해서 REST 키를
`NEXT_PUBLIC_`으로 내보내면 **지오코딩·검색 쿼터를 남이 쓰게 된다.**
변수명에 `JS` / `REST`를 명시해 눈으로 구분되게 한다.

**S-05 (필수)** — 지오코딩(카카오 로컬 REST)은 **서버에서만** 호출한다. 클라이언트에서
주소를 좌표로 바꾸는 코드를 두지 않는다.

### 5.2 B등급 키를 다루는 법

카카오맵 JS 키는 브라우저가 직접 SDK를 부르는 구조라 **숨길 방법이 없습니다.**
"노출되지 않게 한다"가 아니라 "**노출되어도 남이 못 쓰게 한다**"로 목표를 바꿉니다.

**S-06 (필수)** — 카카오 개발자 콘솔 → 앱 설정 → 플랫폼 → **Web 사이트 도메인**에
사용할 도메인만 등록한다.

| 환경 | 등록할 도메인 |
|---|---|
| 로컬 | `http://localhost:3000` |
| 배포 | `https://<프로젝트>.vercel.app` (+ 커스텀 도메인) |

**S-07** — Vercel **프리뷰 배포는 배포마다 URL이 바뀌므로** 등록되지 않아 지도가 뜨지 않는다.
프리뷰에서 지도를 확인하려면 별도 카카오 앱을 만들어 프리뷰용 키를 쓰거나, 프리뷰에서는
지도 없이 목록만 확인한다(F-09가 이를 지탱한다).

**S-08** — 지도가 안 뜨는 사고의 **1순위 원인은 도메인 미등록**이다. F-09의 "지도 로드 실패"
화면에 이 사실을 개발자용 콘솔 로그로 남긴다. (사용자 화면에는 노출하지 않는다)

### 5.3 저장소 위생

**S-09 (필수)** — `.gitignore`로 다음을 제외한다.

```
.env
.env.local
.env.*.local
node_modules/
.next/
```

**S-10 (필수)** — 값이 빈 `.env.local.example`만 커밋해 키 자리를 안내한다.
**첫 커밋 전 `git status`로 `.env.local`이 목록에 없는지 눈으로 확인한다.**

**`.env.local` 형식**

```
# ── A등급: 서버 전용. 절대 NEXT_PUBLIC_ 붙이지 말 것 ──
CULTURE_API_KEY=
DATA_GO_KR_SERVICE_KEY=
KAKAO_REST_API_KEY=

# ── B등급: 브라우저 노출 전제. 카카오 콘솔에서 도메인 제한 필수 ──
NEXT_PUBLIC_KAKAO_MAP_JS_KEY=
```

> **공공데이터포털 서비스키 주의** — Encoding / Decoding 두 가지로 제공된다.
> **어느 쪽을 저장했는지 명확히 하고** 코드에서 이중 인코딩이 일어나지 않게 한다.
> 인증 실패의 가장 흔한 원인이다.

> **이미 커밋해 버렸다면** — `.gitignore`에 추가하는 것만으로는 **이력에서 지워지지 않는다.**
> 해당 키를 **즉시 재발급**하는 것이 유일하게 확실한 대응이다.

**S-11** — Vercel 배포 시 환경변수 4종을 대시보드에 등록한다. 저장소에 올리지 않는다.

---

## 6. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 첫 화면 표시 | 캐시 히트 1.5초 이내, 캐시 미스 6초 이내 |
| 지도 조작 | 마커 1,000개 상황에서도 확대·이동이 끊기지 않을 것 (R-09 클러스터링 전제) |
| 부분 장애 내성 | 한 소스 실패가 전체를 무너뜨리지 않는다 (R-11) |
| 브라우저 | 최신 Chrome / Edge / 모바일 Safari |
| 접근성 | 칩 `aria-pressed`, 목록 항목 키보드 포커스 가능, 에러 `role="alert"`, `prefers-reduced-motion` 대응 |
| 색각 대응 | 카테고리를 **색으로만** 구분하지 않는다 (아이콘 모양 + 텍스트 배지 병기) |
| 지도 대안 | 지도를 못 쓰는 상황에서도 목록 패널로 정보 접근이 가능하다 (F-09) |
| 코드 관리 | Git 버전 관리, 기능 단위 커밋 |

---

## 7. 리스크 및 선결 과제

| ID | 리스크 | 대응 |
|---|---|---|
| **RSK-01** | **문화공공데이터광장에 쓸 만한 전국 맛집 API가 있는지 미확인** | **최우선.** §4.1의 5개 항목을 실측. 커버리지 부족 시 R-03 어댑터에 TourAPI 음식점 소스 추가 |
| **RSK-02** | **시티투어 표준데이터가 정류장 좌표를 주지 않을 수 있음** | 정류장명 지오코딩으로 보정(R-05). 그래도 실패하면 해당 도시는 레이어 없이 맛집 지도만 제공 |
| RSK-03 | 좌표계 불일치로 마커가 엉뚱한 곳에 찍힘 | §2.4의 범위 검증을 정규화 단계에 강제. `proj4` 변환 준비 |
| RSK-04 | 카카오 JS 키 도메인 미등록으로 배포 후 지도가 안 뜸 | S-06~S-08. 배포 직후 지도 표시를 체크리스트로 확인 |
| RSK-05 | 지오코딩 대상이 많아 카카오 로컬 API 쿼터를 소모 | 캐시 파일 우선(R-12). 개발 중 로컬에서 채워 커밋 |
| RSK-06 | 맛집 데이터에 폐업·이전 업소가 섞임 | 공공데이터의 한계. 상세 카드에 갱신 시점과 "방문 전 확인 권장" 캡션 명시 |
| RSK-07 | 지역별 데이터 편차가 커서 강릉 등에서 화면이 비어 보임 | F-04·F-06의 0건 상태를 정식 화면으로 설계. 지역 표시 옆에 건수 병기 |
| RSK-10 | 맛집 API가 **구·군 단위 파라미터를 지원하지 않을 수 있음** | 시/도 단위로 받아 서버에서 주소 문자열로 구·군을 걸러낸다. 주소 표기가 불규칙하면 코드 매칭 실패가 생기므로 STEP 0에서 표본 검증 |
| RSK-11 | 서울 "전체" 등에서 응답이 수천 건이라 전송량·렌더 비용이 큼 | 서버에서 화면에 안 쓰는 필드를 잘라내고, 목록은 페이지네이션(F-07), 마커는 클러스터링(R-09) |
| RSK-08 | 카테고리 매핑 누락으로 "기타"가 과다해짐 | R-08. "기타" 20% 초과 시 매핑 표 보강을 개발 단계에 포함 |
| RSK-09 | 데이터셋 라이선스가 변경금지·비영리 조건일 수 있음 | STEP 0에서 공공누리 조건 확인 후 F-11 표기 확정 |

---

## 8. 개발 단계

> **1차 구현 범위는 §1.4의 "구현 단계" 표를 따른다.** 아래 0~9단계까지가 1차(지도·시티투어 제외),
> 10~11단계(시티투어·정류장 강조)와 8단계 중 지도·마커 부분은 2차로 보류한다.

| 단계 | 내용 |
|---|---|
| **0** | **데이터 실측 검증 (RSK-01, RSK-02)** — §4.1의 5개 항목. 결과로 §2.1·§2.2·§2.3 확정 |
| 1 | 활용신청 및 키 4종 발급 (문화광장 / 공공데이터포털 / 카카오 JS·REST) |
| 2 | 프로젝트 초기화, **`.gitignore` + `.env.local` + `.env.local.example`** (§5) |
| 3 | **`regions.ts`(행정표준코드 기반 시/도 16 + 구·군 트리)** / `categories.ts` 정의, 모의 응답 파일 준비 |
| 4 | 맛집 어댑터 구현 + 정규화 + 좌표 검증 (§2.3, §2.4) |
| 5 | 지오코딩 파이프라인 + 캐시 파일 (R-05, R-12) |
| 6 | 시티투어 파서 (코스·정류장) |
| 7 | 서버 프록시 `/api/region` — 지역 파라미터 검증·병렬 호출·부분 실패·캐시 |
| 8 | 카카오맵 베이스 + 맛집 마커 + 클러스터링 (F-01, F-02) |
| 9 | **지역 2단 선택(F-03) + 카테고리 필터(F-04)** + 목록 패널(F-07) + URL 동기화 |
| 10 | 시티투어 레이어 토글 (F-05) |
| 11 | **정류장 근처 맛집 강조 (F-06)** — 핵심 기능 |
| 12 | 상세 카드 (F-08) |
| 13 | 상태 화면 (F-09) |
| 14 | 반응형 3구간 + 모바일 바텀시트 (F-10) + 접근성 점검 |
| 15 | Vercel 배포, 환경변수 등록, **도메인 등록 확인** (S-06) |
| 16 | README 작성 |

---

## 9. 완료 조건 (Definition of Done)

**기능**

- [ ] 시/도를 고르기 전에는 구·군과 카테고리가 비활성이고 안내 문구가 보인다.
- [ ] 시/도를 고르면 그 시/도의 구·군 목록이 채워지고 맨 앞에 "전체"가 있다.
- [ ] 구·군 "전체"로도 지도와 목록이 정상 동작한다.
- [ ] 시/도를 바꾸면 구·군·카테고리·정류장 선택이 초기화된다.
- [ ] 지역이 확정되면 지도가 해당 지역 중심으로 이동하고 맛집 마커가 표시된다.
- [ ] 카테고리 칩이 지역 확정 후에만 활성화되고, 각 칩에 개수가 병기된다.
- [ ] URL에 지역·카테고리가 반영되어 새로고침해도 같은 화면이 복원된다.
- [ ] 마커 200개를 넘으면 클러스터링된다.
- [ ] 카테고리 칩으로 마커와 목록이 동시에 걸러지며 추가 API 호출이 없다.
- [ ] 시티투어 레이어를 켜고 끌 수 있고, 정류장과 연결선이 보인다.
- [ ] 연결선이 실제 주행 경로가 아니라는 캡션이 있다.
- [ ] **정류장을 누르면 반경 원이 그려지고, 반경 밖 마커가 흐려지며, 목록이 거리순으로 바뀐다.**
- [ ] 반경을 300/500/1000 m로 바꿀 수 있다.
- [ ] 목록 항목을 누르면 상세 카드가 뜨고 지도가 해당 위치로 이동한다.
- [ ] 값이 없는 항목은 "정보 없음"이 아니라 줄 자체가 사라진다.
- [ ] 지오코딩된 항목에 "추정 위치" 캡션이 붙는다.

**견고성**

- [ ] 맛집만 실패해도 시티투어 노선이 보인다. 그 반대도 마찬가지다.
- [ ] 지도 SDK가 로드되지 않아도 목록 패널이 동작한다.
- [ ] 좌표 검증 범위를 벗어난 항목이 지도에 찍히지 않는다.
- [ ] 360 / 768 / 1280 / 1440px에서 가로 스크롤이 없다.
- [ ] 모바일 바텀시트가 지도 조작을 막지 않는다.

**보안 — 이 프로젝트의 합격선**

- [ ] `git status`에 `.env.local`이 잡히지 않는다.
- [ ] 빌드 결과물(`.next/`)과 브라우저 소스에서 **A등급 키 3종이 검색되지 않는다.** ← 실제로 grep으로 확인할 것
- [ ] 브라우저 네트워크 탭의 요청 URL에 서비스키가 보이지 않는다.
- [ ] 카카오 JS 키에 도메인 제한이 걸려 있다.
- [ ] `.env.local.example`이 커밋되어 있고 값은 비어 있다.

**표기**

- [ ] 푸터에 맛집·시티투어 출처와 공공누리 조건이 표기된다.
- [ ] 카카오 저작권 표기가 가려지지 않는다.
- [ ] 카테고리가 재분류 값이며 일부 좌표가 추정임을 밝힌다.

---

## 10. 이후 확장 아이디어 (v2 후보)

| 아이디어 | 메모 |
|---|---|
| 하루 코스 자동 추천 | 정류장 순서 + 식사 시간대를 엮어 "오전 정류장 → 점심 맛집 → 오후 정류장" 생성 |
| 도보 실거리 | 직선거리를 경로 API 기반 도보 거리로 교체 (§2.5의 한계 해소) |
| 대상 지역 확대 | `regions.ts`의 `hasCityTour`·`foodParams`만 채우면 되도록 설계됨 (R-01) |
| 지역 검색창 | 구·군 230개를 셀렉트로 훑는 대신 이름으로 찾기 |
| 현재 위치 기반 | 브라우저 Geolocation → 가장 가까운 정류장 자동 선택 |
| 축제·문화행사 레이어 | 문화공공데이터광장의 다른 데이터셋을 같은 어댑터 구조로 추가 |
| 즐겨찾기 | localStorage 기반. 서버 없이 가능 |
| 맛집 소스 다중화 | R-03 어댑터에 소스를 더해 병합·중복 제거 |
| 다크 모드 | v1은 라이트 고정. 카카오맵 타일과의 조화 검토 필요 |
